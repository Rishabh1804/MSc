# Evidence Report — Destination Normalized Backlog Validation v1

Date: 2026-05-12  
Author: CodeMike  
Dataset: `datasets/reference/destination_expansion_backlog_india_v1_normalized.csv`  
Validator: `src/codemike/data/destination_normalized_validation.py`

## 1. Objective

Validate the normalised 225-row India destination backlog before designing `destinations_master_v2.csv`.

## 2. Status

The normalized validation utility has been created and committed.

The computed validation report can be regenerated by running:

```bash
python src/codemike/data/destination_normalized_validation.py
```

Expected output report:

```text
reports/evidence/destination-normalized-backlog-validation-v1.md
```

## 3. Validator Scope

The validator checks:

- required columns
- candidate ID uniqueness
- duplicate name-country keys
- duplicate name-state keys
- normalized destination types
- normalized vibe tags
- trip-style tags
- context tags
- caution tags
- promotion statuses
- macro-region coverage
- normalisation effect counts

## 4. Promotion Readiness Rule

The normalised backlog is considered clean enough for master promotion design if all of the following are true:

- no missing required columns
- no duplicate candidate IDs
- no duplicate name-country keys
- no duplicate name-state keys
- no invalid normalized destination types
- no invalid normalized vibe tags
- no invalid trip-style tags
- no invalid context tags
- no invalid caution tags
- no invalid promotion statuses

## 5. Current Pipeline Position

```text
candidate backlog ✅
taxonomy QA ✅
normalisation utility ✅
normalised backlog ✅
normalised validation utility ✅
computed normalised validation report ⬅ next execution artifact
master schema design ⬅ next design step
```

## 6. Next Actions

1. Run `src/codemike/data/destination_normalized_validation.py`.
2. Commit the regenerated computed report if it changes.
3. Create `datasets/reference/destinations_master_v2_schema.md`.
4. Create `src/codemike/data/destination_master_promotion.py`.
5. Generate `datasets/reference/destinations_master_v2.csv`.

## 7. Limitation

This report documents the validator and readiness rule. It should be replaced or updated by the computed output from the validation script after execution.
