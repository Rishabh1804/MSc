# Evidence Report — Destinations Master v2 Validation

Date: 2026-05-12  
Author: CodeMike  
Dataset: `datasets/reference/destinations_master_v2.csv`  
Validator: `src/codemike/data/destination_master_validation.py`

## 1. Objective

Validate `destinations_master_v2.csv` against the master v2 schema before creating a browser or downstream scoring layer.

This validation checks structural integrity only. It does not verify live travel facts.

## 2. Status

The validation utility has been created and committed.

Computed validation should be generated by running:

```bash
python src/codemike/data/destination_master_validation.py
```

Expected generated report:

```text
reports/evidence/destination-master-v2-validation-report.md
```

## 3. Validator Scope

The validator checks:

- row count equals 359
- required master columns are present
- no extra columns are present
- unique `destination_master_id`
- unique source lineage keys
- duplicate name-country keys
- duplicate name-state keys
- critical non-blank fields
- valid source layers
- valid destination scales
- valid location types
- valid vibe tags
- valid trip-style tags
- valid context tags
- valid caution tags
- valid verification statuses
- valid promotion statuses
- valid planner-use statuses
- valid source-confidence values

## 4. Expected Readiness

If clean, the generated report should say:

```text
master_structurally_valid_not_planner_ready
```

This means structurally valid, but still not verified for live travel or Planner use.

## 5. Next Actions

1. Run `src/codemike/data/destination_master_validation.py` in Termux.
2. Commit the generated validation report.
3. If clean, create `docs/destination-master-browser-v1.html`.
4. Add the master browser to `docs/index.html`.
5. Begin enrichment planning only after the master browser is available.
