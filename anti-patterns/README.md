# anti-patterns/

Anti-patterns record recurring mistakes, weak methods, misleading practices, and avoidable failures.

Initial targets:

- data leakage
- overfitting
- misleading averages
- vanity dashboard
- premature machine learning
- hard-coded scoring
- unvalidated optimisation

Anti-patterns should link to `FAILURE_LOG.md` when real failures occur.
